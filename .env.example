# JollyKite Environment Variables

# LocalXpose Configuration (Optional - for public tunnel access)
# Uncomment the 'tunnel' service in docker-compose.yml to enable public HTTPS access
# Get your token from: https://localxpose.io/
LX_ACCESS_TOKEN=

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Add your token: LX_ACCESS_TOKEN=your_token_here
# 3. Uncomment tunnel service in docker-compose.yml
# 4. Run: docker-compose up -d
